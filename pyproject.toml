[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=42", "scikit-build", "cmake>=3.15", "ninja", "pandas", "nanobind"]  # Add your build dependencies

[project]
authors = [{name = "Ioannis Magkanaris", email = "ioannis.magkanaris@cscs.ch"}]
description = "A Python package with a CMake project"
name = "icon_benchmark"
version = "0.1.1"

[tool.setuptools]
include-package-data = true
